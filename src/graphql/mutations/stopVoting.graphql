mutation StopVoting($sessionID: ID!) {
    stopVoting(sessionID: $sessionID) {
        id
        name
        votingStarted
        reviewingIssue {
            url
            title
            description
        }
        participants {
            id
            name
            isModerator
            vote {
                points
                abstained
            }
        }
    }
}
