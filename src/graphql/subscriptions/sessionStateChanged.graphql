subscription SessionStateChanged($sessionID: ID!) {
    sessionStateChanged(id: $sessionID) {
        id
        name
        reviewingIssue {
            url
            title
            description
        }
        participants {
            id
            name
            isModerator
            vote {
                points
                abstained
            }
        }
    }
}
